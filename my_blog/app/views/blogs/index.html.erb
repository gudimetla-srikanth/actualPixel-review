<div class="index">
  <div class="index-content">
    <h1>Blogs</h1>
    <div class="index-paginate">
      <%= will_paginate @blogs,class:"digg_pagination"%>
    </div>
    <% @blogs.each do |blog| %>
      <div class="index-box">
        <div class="index-data">
            <h2><%= truncate(blog.title,length:20)%></h2>
          <div class="index-desc"><%= truncate(blog.description,length:30)%></div>
          <div class="index-links">
            <%= link_to "Edit",edit_blog_path(blog),class:"index-btns index-edit"%>
            <%= link_to "Show",blog_path(blog),class:"index-btns index-show" %>
            <%= button_to "Remove",blog_path(blog),method: :delete,data:{ turbo_confirm:"Are you sure?you want to delete?"},class:"index-btns index-delete"%>
          </div>
          <div class="index-creator">
            <% if blog.user %>
            <span><span class="by">by</span><span class="by-name"><%= blog.user.first_name+" "+blog.user.last_name %></span></span>
            <%end%>
          </div>
        </div>
        <div class="index-image">
            <%if blog.image.attached?%>
            <%= link_to image_tag(blog.image,class:"index-img"),blog_path(blog),class:"index-img" %>
            <%end%>
        </div>
      </div>
    <%end%>
  </div>
</div>


