<%= form_with(model: rod) do |form| %>
  <% if rod.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rod.errors.count, "error") %> prohibited this rod from being saved:</h2>

      <ul>
        <% rod.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
